export class fields {
  constructor(public id: number, public attributeid: number, public label: string, public value: any, public key: any, public controlType: string, public category_type: string, public options: string[], public order: string, public type: string, public required: boolean) { }
}
